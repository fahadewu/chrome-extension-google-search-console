# Supabase project config (only the parts this project needs).
# These functions do their own auth (ingest key / admin password), so we turn
# off the platform's default JWT check.

project_id = "search-console-peek"

[functions.ingest]
verify_jwt = false

[functions.admin]
verify_jwt = false
